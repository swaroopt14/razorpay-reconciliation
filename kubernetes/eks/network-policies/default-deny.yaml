# Default deny all ingress AND egress traffic in zord namespace
# Only explicitly allowed traffic will pass
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: zord
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
